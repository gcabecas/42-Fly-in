[project]
name = "fly-in"
version = "1.0.0"
description = "Fly-in project"
requires-python = ">=3.10"
dependencies = [
    "colorbrew>=0.9.0",
]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
]

[tool.uv]
default-groups = ["dev"]
